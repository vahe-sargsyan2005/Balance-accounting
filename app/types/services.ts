export interface IServiceFeature {
    title: string;
    features: string[];
}

export interface IService {
    id: number;
    iconName: string;
    image: string;
    title: string;
    features: string[];
}